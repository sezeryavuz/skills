---
"sezeryavuz-skills": patch
---

`skill-kit`: document installation in its README — add an **Install** section with the `skills` CLI commands, including the `sezeryavuz/skills@skill-kit` by-name form (resolves by `SKILL.md` `name`, so it survives the skill moving folders), the whole-collection form, and the `-g`/`-y` flags.
